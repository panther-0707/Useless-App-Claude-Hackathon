version: '3.8'

services:
  emoji-typing-game:
    build: .
    ports:
      - "5000:5000"
    privileged: true
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
    container_name: emoji_typing_challenge
